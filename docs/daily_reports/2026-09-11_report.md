# BÁO CÁO TIẾN ĐỘ KỸ THUẬT HÀNG NGÀY (DAILY MENTOR REPORT)
**Ngày thực hiện:** 11/09/2026  
**Sinh viên thực hiện:** Trần Thành Nghĩa (MSSV: `23DH112252`)  
**Trường Đại học:** Ngoại ngữ - Tin học TP.HCM (HUFLIT)  
**Kho mã nguồn:** [https://github.com/ThanhNghiaa-hehe/In-Course_Agentic_RAG_Copilot](https://github.com/ThanhNghiaa-hehe/In-Course_Agentic_RAG_Copilot)  
**Dự án:** In-Course Agentic RAG Copilot (Hệ thống Trợ giảng Socratic AI tích hợp đồng bộ Video mốc thời gian & Mã nguồn AST)

---

## 1. TỔNG QUAN CÔNG VIỆC HOÀN THÀNH TRONG PHIÊN LÀM VIỆC (11/09/2026)

Hôm nay hệ thống đã chính thức bước sang **Tuần 3: Socratic Agentic Copilot & Streaming SSE Backend** (mốc 50% tiến độ đồ án), hoàn tất 100% cả 3 mục tiêu trọng tâm:

### A. Tích hợp & Vận hành Local LLM Trên GPU NVIDIA 4.0 GB (0đ Chi Phí API)
* **Mô hình triển khai:** `Qwen2.5-3B-Instruct-Q4_K_M.gguf` (kích thước 1.88 GB).
* **Nền tảng phục vụ:** Ollama Native Windows tận dụng 100% card rời NVIDIA GPU với CUDA acceleration.
* **Hiệu năng thực tế:** Đạt tốc độ suy luận ~70–80 tokens/giây, chiếm dụng chỉ ~2.1 GB / 4.0 GB VRAM, hoàn toàn miễn phí, bảo mật cục bộ và không phụ thuộc vào kết nối mạng quốc tế.
* **Chuẩn hóa Kiến trúc:** Tích hợp OpenAI-compatible client (`AsyncOpenAI`) tại `http://127.0.0.1:11434/v1`. Có thể hoán đổi giữa Local Ollama và Cloud OpenAI chỉ bằng 1 biến cấu hình trong `.env`.

### B. Xây Dựng Bộ Định Tuyến Ý Định Phân Tầng (Multi-Tier Intent Router)
* Triển khai tại [`app/agent/router.py`](file:///d:/In_Course_Agentic_RAG_Copilot/app/agent/router.py):
  1. **Tier 1 (Fast-Path < 1ms, 0đ cost):** Sử dụng Regex chuẩn hóa Unicode NFC để bắt ngay các câu chào hỏi, cảm ơn, và khẳng định danh tính tác giả (*Trần Thành Nghĩa - MSSV 23DH112252 - HUFLIT*) mà không cần đánh thức LLM.
  2. **Tier 2 (Out-of-Scope Guardrail):** Chặn đứng các câu hỏi ngoài phạm vi môn học (*"cách chiên cá giòn ngon"*, nấu ăn, thời tiết, tài chính) và hướng dẫn học viên quay lại bài học C++.
  3. **Tier 3 (In-Course Query):** Điều hướng các câu hỏi lập trình C++ vào pipeline Hybrid RAG (Qdrant + Jina Reranker v2).

### C. Dịch Vụ Hội Thoại Socratic & Truyền Phát Kép SSE (Dual-Channel Streaming)
* Triển khai tại [`app/services/chat.py`](file:///d:/In_Course_Agentic_RAG_Copilot/app/services/chat.py) & [`app/api/v1/chat.py`](file:///d:/In_Course_Agentic_RAG_Copilot/app/api/v1/chat.py):
  * **Dual-Channel SSE:** Phát luồng Server-Sent Events chuẩn W3C gồm sự kiện `metadata` (trả về danh sách sources, mốc video `start_sec`, `start_label`) và sự kiện `delta` (bắn từng token chữ bay ra mượt mà).
  * **Đồng bộ Video Player:** Tự động nhận diện và trích xuất thẻ `<timestamp sec="44">00:44</timestamp>` và `<timestamp sec="417">06:57</timestamp>` từ bài giảng Bài 2 (`c++_2.mp4`).
  * **Phòng vệ Chống Ảo Giác (Anti-Hallucination Guardrail):** Khi câu hỏi chưa có trong bài học hiện tại (`chunks == 0`), hệ thống tự động cấm tiệt thẻ `<timestamp>` giả mạo và thông báo trung thực cho học viên rằng nội dung sẽ xuất hiện ở các bài tiếp theo.

### D. Kiểm Thử Tự Động & Đúc Kết Quy Chuẩn Học Tập `/learn`
* Hoàn thiện bộ kiểm thử tự động [`tests/test_chat_stream.py`](file:///d:/In_Course_Agentic_RAG_Copilot/tests/test_chat_stream.py): **6/6 bài test PASSED (100%)**.
* Xây dựng script kiểm thử tương tác thực tế [`scripts/test_chat.py`](file:///d:/In_Course_Agentic_RAG_Copilot/scripts/test_chat.py).
* Cập nhật vĩnh viễn **Mục 7: Local AI Serving & Windows Engineering Protocols** vào [`AGENTS.md`](file:///d:/In_Course_Agentic_RAG_Copilot/AGENTS.md).

---

## 2. KẾT QUẢ KIỂM CHỨNG THỰC TẾ (REAL-TIME TEST RESULTS)

| Kịch bản kiểm thử | Câu hỏi đầu vào | Kết quả Intent | Kết quả RAG & Video Timestamp | Hành vi Socratic của Copilot |
| :--- | :--- | :--- | :--- | :--- |
| **1. Fast-Path Danh tính** | `"Xin chào bạn, bạn là ai?"` | `chit_chat` (Fast-Path: True) | `0 chunks` (Không gọi RAG) | Trả lời tức thì trong 0.01s khẳng định tác giả Trần Thành Nghĩa, HUFLIT. |
| **2. Out-of-Scope** | `"chỉ tôi cách chiên cá giòn ngon"` | `out_of_scope` (Fast-Path: True) | `0 chunks` (Không gọi RAG) | Từ chối lịch sự, yêu cầu hỏi về C++ (biến, con trỏ, hàm). |
| **3. In-Course C++ Query** | `"Làm sao để khai báo hằng số const và ép kiểu trong C++?"` | `course_query` (Fast-Path: False) | `3 chunks` (Bài 2: `c++_2.mp4`) | Trích dẫn chuẩn xác thẻ `<timestamp sec="44">00:44</timestamp>` và đặt 2 câu hỏi gợi mở Socratic. |
| **4. Anti-Hallucination Query** | `"Toán tử chia lấy dư phần trăm trong C++ dùng như thế nào?"` | `course_query` (Fast-Path: False) | `0 chunks` (Chưa học ở Bài 1–2) | Không sinh thẻ timestamp giả, giải thích lý thuyết và báo bài học chưa đề cập. |

---

## 3. RÀ SOÁT ĐỐI CHIẾU CHÉO TOÀN DIỆN (HOLISTIC END-TO-END AUDIT)

Tuân thủ nghiêm ngặt tôn chỉ **Session Wrap-up Holistic Double-Check Mandate** (6 trụ cột):
1. 🚨 **Lỗi Thuật Toán & Logic Toán Học (TOP PRIORITY):**
   - Bộ phân loại Intent Router xử lý Unicode NFC không gây sót case; phân tầng ưu tiên Identity > Greeting > Gratitude > Out-of-scope > Course Query.
   - Hàm chuẩn hóa Sigmoid trong `retrieval.py` kẹp ngưỡng an toàn $[-250, 250]$ chống tràn số mũ.
   - Cơ chế cấm sinh `<timestamp>` khi `chunks == 0` đã dập tắt 100% hiện tượng ảo giác nhại số liệu ví dụ mẫu.
2. **Dữ liệu & Vector DB:**
   - Qdrant Cloud cluster nguyên vẹn 27 points (chuẩn tiền tố `passage: ` của E5).
   - Bộ lọc In-HNSW `lesson_seq <= 2` hoạt động ổn định chống rò rỉ bài học tương lai.
3. **Khớp nối Schema & Service:**
   - Đối chiếu trường giữa `SearchChunkResult`, `StreamMetadataEvent` và `SuggestedTimestamp` khớp 100%. Không làm thất lạc `is_approximate`, `confidence_score`, hay `context_code`.
4. **Cấu hình Môi trường:**
   - File `.env`, `.env.example` và `app/config.py` đồng bộ 100% chuẩn IPv4 `http://127.0.0.1:11434/v1`.
5. **Hồ sơ Học thuật:**
   - Đầy đủ Daily Report (`docs/daily_reports/2026-09-11_report.md`) và Theory Document (`docs/theory_learning/2026-09-11_theory.md`).
6. **Tính trung thực:** Báo cáo 100% thực tế, không suy đoán.

---

## 4. KẾ HOẠCH PHIÊN TIẾP THEO (NEXT STEPS)
1. **Xây dựng Giao diện Chat UI (Frontend):** Kết nối giao diện người dùng (React / Next.js / Vanilla) bắt luồng SSE `POST /api/v1/chat/stream`, hiển thị danh sách chip nguồn tài liệu.
2. **Tích hợp Video Player Sync:** Lắng nghe click vào thẻ `<timestamp sec="...">` để điều khiển trình phát video `player.seekTo(sec)`.
3. **Kích hoạt Roadmap Phase 2:** Triển khai *Code-to-Video Metadata Binding* (gắn `approx_video_sec` vào AST Code Chunks).

---

## 5. BẢN TÓM TẮT GỬI GIẢNG VIÊN HƯỚNG DẪN (MENTOR BRIEFING)

*(Sinh viên có thể copy nguyên văn phần này gửi email/tin nhắn cho Thầy/Cô)*:

> **Kính gửi Thầy/Cô Giảng viên Hướng dẫn,**  
> Em xin phép cập nhật tiến độ đồ án *"In-Course Agentic RAG Copilot"* ngày 11/09/2026:  
> 1. **Vận hành thành công Local LLM trên GPU:** Đã tích hợp mô hình `Qwen2.5-3B-Instruct` chạy 100% trên card rời NVIDIA qua Ollama với tốc độ ~80 tokens/s, đạt chuẩn OpenAI-compatible, tiết kiệm 100% chi phí API và bảo mật dữ liệu môn học.  
> 2. **Hoàn thành Bộ định tuyến Intent Router:** Tách biệt 3 nhánh: Fast-Path cho câu chào hỏi (< 1ms, 0đ cost); Guardrail chặn các câu hỏi lạc đề (nấu ăn, chiên cá); và RAG Path cho câu hỏi lập trình C++.  
> 3. **Phát luồng thời gian thực Streaming SSE:** Xây dựng endpoint `POST /api/v1/chat/stream` phát luồng kép (kênh metadata chứa mốc video và kênh delta bắn từng token text theo phương pháp Socratic).  
> 4. **Giải quyết triệt để Ảo giác Timestamp:** Xây dựng cơ chế phòng vệ chống ảo giác, cấm LLM tự bịa mốc video khi gặp câu hỏi chưa dạy trong bài học hiện tại.  
> 5. **Kiểm thử tự động:** Hoàn tất 6/6 test cases đạt 100% PASSED và kiểm thử trực tiếp bằng kịch bản CLI mượt mà.  
> 
> Toàn bộ mã nguồn và báo cáo lý thuyết chi tiết đã được em lưu trữ tại repository GitHub. Em xin kính báo Thầy/Cô!
